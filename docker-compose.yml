services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.bot
    env_file:
      - .env
    working_dir: /app
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    command: ["python", "-m", "socialosintagent.discord_handler"]
    restart: unless-stopped

